version: 0.0
os: linux
files:
  - source: /
    destination: /django/new_chatapp/fundoo
hooks:
  BeforeInstall:
    - location: scripts/before_install.sh
      timeout: 500
      runas: root
  AfterInstall:
    - location: scripts/django.sh
      timeout: 800
      runas: root
